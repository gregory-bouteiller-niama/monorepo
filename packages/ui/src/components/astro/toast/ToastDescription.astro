---
import type { HTMLAttributes } from "astro/types";
import { toastDescription } from "../toast.ts";

type Props = HTMLAttributes<"div">;

const { class: className, ...rest } = Astro.props;
---

<div class:list={[toastDescription(), className]} data-toast-description {...rest} data-slot="toast-description"><slot /></div>
