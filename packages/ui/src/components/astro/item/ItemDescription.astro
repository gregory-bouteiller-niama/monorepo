---
import type { HTMLAttributes } from "astro/types";
import { itemDescription } from "./index.ts";

type Props = HTMLAttributes<"p">;

const { class: className, ...rest } = Astro.props;
---

<p class:list={[itemDescription(), className]} {...rest} data-slot="item-description"><slot /></p>
