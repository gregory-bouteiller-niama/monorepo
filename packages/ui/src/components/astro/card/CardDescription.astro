---
import type { HTMLAttributes } from "astro/types";
import { cardDescription } from "../card.ts";

type Props = HTMLAttributes<"div">;

const { class: className, ...rest } = Astro.props;
---

<div class:list={[cardDescription(), className]} {...rest} data-slot="card-description"><slot /></div>
