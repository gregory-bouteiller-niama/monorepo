import { cva } from "class-variance-authority";

// STYLES ----------------------------------------------------------------------------------------------------------------------------------
export const PUBLIC_LAYOUT = {
  skipLink: cva("fixed top-4 left-4 z-60 -translate-y-[200%] focus-visible:translate-y-0 motion-reduce:transition-none"),
};
