import { ContactForm as ContactFormNative } from "@niama/ui/react/contact-form";

// MAIN ------------------------------------------------------------------------------------------------------------------------------------
export function ContactForm() {
	return <ContactFormNative onSubmit={async () => {}} />;
}
